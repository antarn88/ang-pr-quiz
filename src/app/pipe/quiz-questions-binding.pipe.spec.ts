import { QuizQuestionsBindingPipe } from './quiz-questions-binding.pipe';

describe('QuizQuestionsBindingPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizQuestionsBindingPipe();
    expect(pipe).toBeTruthy();
  });
});
